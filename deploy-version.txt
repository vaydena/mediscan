mediscan
build: ms-v6-onescan-3d7a9c
date: 2026-09-04
note: Ein Scan liest alles + nur EIN Button. (1) REGRESSION behoben: In ms-v5 war der Foto-Button ein <label for="barfile"> auf einen per hidden-Attribut (display:none) versteckten Datei-Input. Auf Samsung Internet/manchen Android-Browsern oeffnet ein solches Label KEINEN Kamera-/Dateidialog -> der Button tat gar nichts. Zurueck auf den in der Original-App bewaehrten Weg: echter <button id="scanPhotoBtn"> + photoBtn.addEventListener("click", () => barfile.click()); der programmatische Klick aus einer echten Nutzergeste heraus oeffnet die Kamera auch bei display:none-Input zuverlaessig. (2) Nur noch EIN Scan-Button "Packung scannen": der separate Live-Scanner-Weg ist aus der UI entfernt, ebenso das getrennte Texterkennungs-Feld. (3) EIN Scan liest alles: der Schnappschuss laeuft zuerst durch BarcodeDetector/ZXing (1D-PZN-Strichcode code_39 + 2D-Data-Matrix securPharm) und die PZN-Extraktion (Label "PZN 12345678" / deutsches NTIN 4150######## / Pruefziffer-Fallback, faengt auch PPN-eingebettete PZN). Ist KEIN Code lesbar, wird DERSELBE Schnappschuss automatisch per Tesseract-Texterkennung nach dem Praeparatenamen gelesen -> Code zuerst, Name als Rueckfall, ohne zweiten Knopf. Manuelle PZN-Eingabe nur noch als aufklappbares <details> ("Kein Code lesbar?"). sw.js VERSION -> ms-v1-2026-09-04-7. Verifiziert per jsdom-Harness (Button-Klick oeffnet #barfile, Positiv-Decode, OCR-Rueckfall via Tesseract-Shim) und engine-smoke.
note_prev: Foto-Scan-Knopf robuster + Update-Selbstheilung. app.html + index.html laden die Seite EINMAL automatisch neu, sobald ein neuer Service-Worker die Kontrolle uebernimmt (controllerchange; nur bewaffnet, wenn schon ein SW aktiv war). build ms-v5-scanfix-4f1c7d, sw.js ms-v1-2026-09-04-6.
note_prev2: Barcode-/PZN-Scan gegen Androids Overlay-/Tapjacking-Sperre robust gemacht: Hauptweg <input type=file capture=environment> oeffnet die native Kamera (kein getUserMedia), Standbild -> BarcodeDetector offline / ZXing online. build ms-v4-fotoscan-5b7d2a, sw.js ms-v1-2026-09-04-5.
