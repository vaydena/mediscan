mediscan
build: ms-v21-ocr-warmup-c7d2
date: 2026-09-15
note: OCR-KETTE VORWAERMEN (erster Scan haengt nicht mehr an einem Just-in-time-CDN-Abruf). Engine/DB inhaltlich UNBERUEHRT, keine erfundenen klinischen Daten, kein Anti-Fabrikations-Waechter gelockert. NACHTRAG zum SW-Cache-Fix (ms-v20): Die Tesseract-Kette wird jetzt bereits beim ANTIPPEN von "Foto aufnehmen"/"Aus Galerie ..." vorgewaermt - also PARALLEL zum OS-Bildpicker, der den Nutzer mehrere Sekunden kostet. In dieser Zeit laedt ein Wegwerf-Worker Skript + Worker + WASM-Core + ~15 MB deu.traineddata in den dauerhaften OCR-Cache (mediscan-ocr-v1) und wird sofort wieder freigegeben. Dadurch findet AUCH der erste Scan einer Sitzung die Teile bereits im Cache -> schneller UND robuster; der einzige nach ms-v20 verbliebene Rest-Ausfallpunkt (die 15-MB-Sprachdaten wurden bisher erst IM Moment der Texterkennung geholt) ist damit geschlossen. REIN ADDITIV: der Scan-Pfad decodeImageFile -> runOCR ist unveraendert, warmOCR() ist best-effort (Fehler verschluckt, guard ocrWarmed erlaubt Wiederholung beim naechsten Antippen), der regulaere Scan behaelt seine 3 Wiederholungen. Gilt fuer BEIDE Wege (Kamera #barfileCam UND Galerie #barfileGal). VERIFIZIERT: engine-smoke.js 122 ok/0 fail (unveraendert), node --check ms-app.js ok. DB ms-db-2026-09-08-2 (unveraendert), SW-Cache ms-v1-2026-09-15-31, OCR-Cache mediscan-ocr-v1 (unveraendert).
note_prev: OCR-ROBUSTHEIT + ANTI-PHANTOM (Ursache der "2 von 8: gar nichts erkannt" behoben; Engine/DB inhaltlich UNBERUEHRT, nur der Anti-Fabrikations-Waechter gestaerkt, keine erfundenen klinischen Daten). URSACHE: Die Detektion ist deterministisch 8/8 - die 2 Fehllaeufe waren KEINE Erkennungsfehler, sondern komplette OCR-Ausfaelle: die Tesseract-Kette (tesseract.min.js, worker.min.js, WASM-Core, deu.traineddata) wurde bei JEDEM Scan frisch live von cdn.jsdelivr.net geholt; ein einzelner CDN-/Netzaussetzer liess runOCR sofort werfen -> 0 Treffer (intermittierend, erklaert genau 2 von 8). FIX (1) SERVICE-WORKER: cdn.jsdelivr.net jetzt cache-first in einen dauerhaften, versionsunabhaengigen OCR-Cache (mediscan-ocr-v1), inkl. OPAKER Antworten (no-cors-importScripts der WASM-Core im Worker) - nach dem ersten Online-Scan laeuft die Texterkennung offline/robust, kein Live-Fetch je Scan mehr. (2) APP: loadTesseract() und neuer startOCRWorker() mit je 3 Wiederholungen (Backoff); Script-Tag crossOrigin=anonymous -> CORS-saubere, cachebare Antwort; Woerterbuch-AUS jetzt korrekt bei der Worker-Init (load_system_dawg/-freq_dawg sind in Tesseract v5 init-only). (3) ENGINE Anti-Phantom (STAERKUNG, keine Lockerung): neue COMMON_DE-Stoppwort-Sperre - haeufige deutsche Plan-/Funktionswoerter duerfen keinen Fuzzy-Treffer SAEEN; das Token "alle" aus der Dosier-Spalte "alle 12 Std." fabriziert nicht mehr "allo"=Allopurinol. Substring-/Exakt-Stufen unberuehrt, echte Wirkstoffe weiter ueber ihren echten Namen; Johanniskraut/Laif (Hypericum perforatum) bleibt korrekt erkannt. VERIFIZIERT: engine-smoke.js 122 ok/0 fail. DB ms-db-2026-09-08-2 (unveraendert), SW-Cache ms-v1-2026-09-15-30.
note_prev2: GALERIE-/KAMERA-SCAN REPARIERT (Kernbug: Foto des Medikationsplans zeigte KEINE Wechselwirkungen). Drei Ursachen behoben, Engine + DB inhaltlich UNBERUEHRT (keine erfundenen klinischen Daten). (1) DATA-MATRIX-FALLE: Der 2D-Code des Bundeseinheitlichen Medikationsplans (BMP) enthaelt Markup (<MP><M p=PZN w=Wirkstoff .../>); pzn.parse() fischte daraus eine pruefsummen-gueltige Einzel-PZN, worauf decodeImageFile ANHIELT und OCR nie startete -> 0 Treffer. Fix: looksStructured()-Waechter laesst pzn.parse() bei Markup null zurueckgeben; neuer bmp.text()-Pfad erntet stattdessen alle Wirkstoff-/Handelsnamen aus den Attributen. (2) KEINE AUTO-ANALYSE: analyzeIfReady() jetzt in alle vier Decode-Pfade (Standbild + Live-Kamera, je nativ BarcodeDetector und ZXing) und in den OCR-Rueckfall eingehaengt -> Wechselwirkungen erscheinen sofort beim ERSTEN Scan. (3) FUZZY-ERFINDUNG: Levenshtein-Toleranz 3->2 gesenkt + Token-Exklusivitaets-Waechter -> exakt getroffene Wirkstoffe koennen keine lev-3-Nachbarn mehr miterfinden. Verifiziert am ARMIN-Musterplan: engine-smoke.js 116 ok/0 fail, Ende-zu-Ende-Repro liefert exakt die 3 belegten Wechselwirkungen (Simvastatin+Clarithromycin, Insulin+Metoprolol, Pantoprazol+Clopidogrel), 0 erfundene, pzn.parse(BMP)->null. DB ms-db-2026-09-08-2 (unveraendert), SW-Cache ms-v1-2026-09-15-29.
note_design: DESIGN-UPLIFT (Design-System v2 "Clinical Premium"): Logo-Palette Petrol/Mint/Amber vereinheitlicht, Dark Mode (body.ms-app), Marken-Buttons/48px/Fokus-Ringe, Sticky-CTA, Scan-Panel, Verdict-Banner (nur DB-Fakten). Engine/DB unberuehrt. build ms-v15-design-7b4e, sw.js ms-v1-2026-09-08-25.
