mediscan
build: ms-v5-scanfix-4f1c7d
date: 2026-09-04
note: Foto-Scan-Knopf robuster + Update-Selbstheilung. (1) Nach einem App-Update konnte der neue Button "Barcode / PZN fotografieren" ins Leere klicken (kein Kameradialog, keine Meldung), weil die frisch (netzwerk-first) geladene app.html mit einer noch aus dem alten Cache (cache-first) stammenden ms-app.js lief -> neuer Button, alte Logik ohne Verdrahtung. Fix: app.html + index.html laden die Seite EINMAL automatisch neu, sobald ein neuer Service-Worker die Kontrolle uebernimmt (navigator.serviceWorker controllerchange; nur bewaffnet, wenn schon ein SW aktiv war -> kein Reload beim Erstbesuch, garantiert einmalig). (2) Der Foto-Button ist jetzt ein natives <label for="barfile"> -> oeffnet die System-Kamera OHNE JavaScript (robust auch bei noch nicht aktualisiertem Script) und ohne den .click()-auf-hidden-input-Umweg. Der JS-Klick-Handler wurde entfernt (haette die Kamera sonst doppelt geoeffnet); das Auslesen (change -> decodeImageFile) bleibt, plus Tastatur-Aktivierung (Enter/Space) fuer A11y. sw.js VERSION -> ms-v1-2026-09-04-6. Verifiziert per jsdom-Harness (Label-Verdrahtung + Negativ/Positiv-Decode) und engine-smoke.
note_prev: Barcode-/PZN-Scan gegen Androids Overlay-/Tapjacking-Sperre robust gemacht: Hauptweg <input type=file capture=environment> oeffnet die native Kamera (kein getUserMedia), Standbild -> BarcodeDetector offline / ZXing online. Live-Scanner nur noch sekundaer. build ms-v4-fotoscan-5b7d2a, sw.js ms-v1-2026-09-04-5.
note_prev2: Startseite (index.html) komplett neu im Vaydena-Design (Petrol/Gold/Navy). build ms-v3-landing-9c4e2a, sw.js ms-v1-2026-09-04-4.
