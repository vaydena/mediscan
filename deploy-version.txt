mediscan
build: ms-v7-gallery-2a9f1c
date: 2026-09-04
note: Scan liest jetzt auch bereits vorhandene Fotos aus der Galerie (wie in der Original-App). Ursache/Fix: der Datei-Input #barfile hatte capture="environment"; dieses Attribut zwingt Android/iOS direkt in die Kamera und UNTERDRUECKT die Galerie-Auswahl. capture ENTFERNT -> ein Klick auf den EINEN Button "Packung scannen" oeffnet weiterhin per barfile.click() den System-Dialog, der nun BEIDES anbietet: neues Foto mit der Kamera aufnehmen ODER ein vorhandenes Bild aus der Galerie waehlen. Beide Quellen laufen durch denselben Decoder (BarcodeDetector/ZXing: 1D-PZN-Strichcode code_39 + 2D-Data-Matrix; PZN-Extraktion Label/NTIN 4150########/Pruefziffer) und fallen, wenn kein Code lesbar ist, automatisch auf Tesseract-Texterkennung des Praeparatenamens zurueck. Weiterhin nur EIN Button, kein Alternativweg-Zwang. sw.js VERSION -> ms-v1-2026-09-04-8. Verifiziert per jsdom-Harness (barfile OHNE capture, Button-Klick oeffnet #barfile, Positiv-Decode PZN, OCR-Rueckfall) und engine-smoke.
note_prev: Ein Scan liest alles + nur EIN Button. REGRESSION behoben: in ms-v5 war der Foto-Button ein <label for="barfile"> auf einen per hidden (display:none) versteckten Input -> oeffnete auf Samsung Internet KEINEN Dialog (toter Knopf). Zurueck auf echten <button id="scanPhotoBtn"> + photoBtn.addEventListener("click", () => barfile.click()). EIN Scan liest 1D-PZN + 2D-Data-Matrix und faellt bei keinem Code automatisch auf Tesseract-OCR des Namens zurueck. build ms-v6-onescan-3d7a9c, sw.js ms-v1-2026-09-04-7.
note_prev2: Foto-Scan-Knopf robuster + Update-Selbstheilung. app.html + index.html laden die Seite EINMAL automatisch neu, sobald ein neuer Service-Worker die Kontrolle uebernimmt (controllerchange). build ms-v5-scanfix-4f1c7d, sw.js ms-v1-2026-09-04-6.
