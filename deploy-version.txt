mediscan
build: ms-v8-camgal-6d41e2
date: 2026-09-04
note: Foto-Scan bietet jetzt beide Wege frei zur Auswahl: ZWEI Buttons "Foto aufnehmen" (Kamera) und "Aus Galerie waehlen" (vorhandenes Bild). Ursache/Fix: der vorige Ein-Input-Ansatz ohne capture (ms-v7) hat auf modernen Android-Versionen accept="image/*" ZWINGEND in den Android-Foto-Picker geroutet -> nur Galerie, KEINE Kamera mehr. Ein einzelner Datei-Input kann daher nicht zuverlaessig beides anbieten. Loesung: je ein eigener Input - #barfileCam mit capture="environment" (oeffnet direkt die Kamera) und #barfileGal ohne capture (oeffnet den Foto-/Datei-Picker mit vorhandenen Bildern). Beide werden per .click() aus einer echten Nutzergeste geoeffnet (funktioniert auch bei display:none und auf Samsung Internet) und laufen durch DENSELBEN Decoder (BarcodeDetector/ZXing: 1D-PZN-Strichcode code_39 + 2D-Data-Matrix; PZN-Extraktion Label/NTIN 4150########/Pruefziffer) mit automatischem Tesseract-OCR-Rueckfall auf den Praeparatenamen, wenn kein Code lesbar ist. sw.js VERSION -> ms-v1-2026-09-04-9. Verifiziert per jsdom-Harness (beide Buttons vorhanden, beide oeffnen ihren Input, Kamera-Input MIT capture, Galerie-Input OHNE capture, Positiv-Decode PZN, OCR-Rueckfall) und engine-smoke.
note_prev: Galerie-Fotos scannen. capture="environment" entfernt, damit der System-Dialog auch vorhandene Bilder anbietet - hat aber auf modernen Android-Versionen die Kamera verdraengt (Foto-Picker only). build ms-v7-gallery-2a9f1c, sw.js ms-v1-2026-09-04-8.
note_prev2: Ein Scan liest alles + nur EIN Button. REGRESSION behoben: in ms-v5 war der Foto-Button ein <label for="barfile"> auf einen per hidden (display:none) versteckten Input -> oeffnete auf Samsung Internet KEINEN Dialog (toter Knopf). Zurueck auf echten <button> + barfile.click(). build ms-v6-onescan-3d7a9c, sw.js ms-v1-2026-09-04-7.
