mediscan
build: ms-v9-noscantab-3e8a17
date: 2026-09-04
note: Die beiden Foto-Buttons "Foto aufnehmen" (Kamera) und "Aus Galerie waehlen" erscheinen jetzt SOFORT auf der Seite. Der bisherige Tab-Umschalter mit dem Button "Foto scannen" (#tab-scan) ist entfernt: statt Suche/Foto hinter zwei Tabs (#tab-manual/#tab-scan, #pane-scan war hidden) stehen beide Eingabewege gleichzeitig untereinander - oben "Nach Name suchen", darunter per Trennlinie "... oder Packung fotografieren" mit den zwei Buttons. HTML: .seg-Tablist raus, #pane-scan ohne hidden, je eine kleine <h3>-Ueberschrift + .hr-Trenner. JS: setTab() ist ein sanfter No-Op geworden (beendet nur die Live-Kamera und fokussiert das Suchfeld, blendet nichts mehr aus), Tab-Klick-Verdrahtung in wire() entfernt; alle Aufrufer nutzen weiterhin setTab("manual"). Foto-Decoder (Kamera+Galerie, BarcodeDetector/ZXing 1D-PZN+2D-Data-Matrix, Tesseract-OCR-Rueckfall auf den Namen) unveraendert. sw.js VERSION -> ms-v1-2026-09-04-10. Verifiziert per jsdom-Harness 28/28 (kein #tab-scan/#tab-manual, #pane-scan nicht hidden, beide Buttons+Inputs, Positiv-Decode ueber Kamera UND Galerie, OCR-Rueckfall) und engine-smoke 63/63.
note_prev: Kamera UND Galerie frei waehlbar. Zwei getrennte Inputs - #barfileCam mit capture="environment" (Kamera) und #barfileGal ohne capture (Foto-Picker) - beide per .click() aus Nutzergeste, gemeinsamer Decoder. Ursache: moderne Android-Versionen routen accept="image/*" OHNE capture zwingend in den Galerie-Foto-Picker, ein Einzel-Input kann daher nicht zuverlaessig beides. build ms-v8-camgal-6d41e2, sw.js ms-v1-2026-09-04-9.
note_prev2: Galerie-Fotos scannen. capture="environment" entfernt - hat auf modernen Android-Versionen aber die Kamera verdraengt (Foto-Picker only). build ms-v7-gallery-2a9f1c, sw.js ms-v1-2026-09-04-8.
