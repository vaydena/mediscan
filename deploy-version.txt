mediscan
build: ms-v12-appicon-7f3e
date: 2026-09-04
note: Startbildschirm-"App-Button": MediScan laesst sich jetzt wie eine App auf dem Handy ablegen und oeffnet mit einem Tipp direkt den Foto-Scan. DREI Bausteine. (1) MANIFEST: start_url -> "./app.html?go=scan" (die abgelegte Verknuepfung landet also gleich beim Scan) + neuer "shortcuts"-Eintrag "Scannen" (Android-Long-Press-Schnellaktion, ebenfalls ?go=scan); icon-192/512 mit purpose:"any" ausgezeichnet. (2) INSTALL-HILFE in app.html (bewusst INLINE gehalten, nicht in der gecachten ms-app.js, damit die Logik nach Deploy sofort mit dem frischen HTML kommt und nicht an der SW-Cache-Kohaerenz-Falle haengt): fruehes Abfangen von beforeinstallprompt im <head> (Chromium feuert das Event nur einmal, sehr frueh) + Banner #ms-install unter dem Sicherheits-Disclaimer. Android/Chromium bekommt einen echten "Installieren"-Button (ruft das aufgeschobene Prompt auf); iOS/iPadOS (kein Prompt-API, iPadOS tarnt sich als Mac -> ontouchend-Erkennung) bekommt stattdessen die Kurzanleitung "Teilen-Symbol -> Zum Home-Bildschirm"; laeuft die App bereits im Standalone-Modus oder wurde sie installiert bzw. der Hinweis mit x weggeklickt (persistiert in localStorage "ms.install.hidden"), erscheint gar nichts. (3) DEEP-LINK zum Scan: bei ?go=scan oder #scan wird der Scan-Bereich #pane-scan eingeblendet, sanft zentriert und kurz per Puls (@keyframes msScanPulse, reduced-motion-fest) hervorgehoben; danach wird der go-Parameter per history.replaceState wieder aus der Adresse genommen (Reload/Teilen startet normal). BEWUSST KEIN Auto-Start der Kamera - die braucht eine echte Nutzergeste und wuerde von Android/iOS ohnehin blockiert. Reine UI-/PWA-Aenderung, Engine und Referenz-DB unveraendert. sw.js VERSION -> ms-v1-2026-09-04-21. Verifiziert: manifest.webmanifest valides JSON (start_url + 1 shortcut), alle 4 Inline-Skripte kompilieren, engine-smoke 82/82 (Engine unberuehrt), jsdom-Smoke 20/20 ueber 5 Szenarien (go=scan->#pane-scan bekommt .ms-focus + go-Param entfernt; beforeinstallprompt->Banner+Installieren-Button+prompt(); iOS->Anleitung statt Button; abgelehnt->still, Scan-Fokus bleibt; standalone->Banner unterdrueckt).
note_chip: Medikamenten-Chip in "Ihre Medikamente" zeigte bei Generika den Namen doppelt (z. B. "Levothyroxin  Levothyroxin"), weil er Name + Wirkstoff fest untereinander rendert und bei Generika name===activeIngredient ist. Fix in renderChips (ms-app.js): die Wirkstoff-Unterzeile wird nur noch gezeigt, wenn sie sich vom Namen unterscheidet (Marken wie Aspirin->Acetylsalicylsaeure/Concor->Bisoprolol behalten die nuetzliche Angabe; 173 von 252 Generika-Chips sind die Dopplung jetzt los). Reine Darstellungskorrektur, keine Daten-/Engine-Aenderung. sw.js VERSION -> ms-v1-2026-09-04-20. Verifiziert: engine-smoke 82/82 (Engine unberuehrt) + Guard-Logik gegen die echte DB geprueft.
note_ocr: OCR-Texterkennung (Foto-Rueckfall, wenn kein Barcode/PZN/Data-Matrix lesbar ist) deutlich verbessert. DREI Baustellen: (1) BILD-AUFBEREITUNG vor Tesseract - Handy-Fotos werden auf 1800px Kante herunterskaliert, in Graustufen gewandelt und der Kontrast auf das 2.-98. Perzentil gespreizt (robust gegen Glanzlichter auf Blister/Folie); fail-safe: schlaegt ein Schritt fehl, geht das Originalbild unveraendert an die Erkennung, der Scan bricht nie. (2) TESSERACT-TUNING - Woerterbuch AUS (load_system_dawg=0/load_freq_dawg=0), damit Wirkstoff-/Markennamen nicht zu deutschen Woerterbuchwoertern "korrigiert" werden; pageseg_mode=6 (zusammenhaengender Textblock), preserve_interword_spaces=1. (3) ROBUSTERES MATCHING in der Engine (detect): zusaetzlich ein trennzeichen-toleranter Substring-Abgleich ("verklebte" Fassung ohne Space/Bindestrich) faengt Namen, die die OCR getrennt liest ("ben u ron", "beloc zok", "parace tamol"); reine Ziffernfolgen (Dosierungen 400/1000/...) werden aus dem Fuzzy herausgefiltert. Neu ausserdem: bei Nichttreffer zeigt die App den tatsaechlich GELESENEN Rohtext ("Erkannter Text: ...") an, damit der Nutzer gezielt ueber die Suche nachhelfen kann (neues #ocrraw-Element in app.html). Erkennung ist quellenunabhaengig - Kamera- wie Galerie-Foto. sw.js VERSION -> ms-v1-2026-09-04-11. Verifiziert: engine-smoke 67/67 (inkl. neuer Regressionstests fuer getrennt gelesene Namen + reine-Ziffern-Filter) und jsdom-App-Harness 11/11 (Aufbereitung laeuft & Canvas geht an recognize, Woerterbuch AUS, Kontrastspreizung 60->0/190->255, Treffer->Chip, Nichttreffer->Rohtext sichtbar).
note_prev: Beide Foto-Buttons "Foto aufnehmen" (Kamera) und "Aus Galerie waehlen" erscheinen sofort; Tab-Umschalter mit "Foto scannen" (#tab-scan) entfernt, Suche und Foto stehen gleichzeitig. build ms-v9-noscantab-3e8a17, sw.js ms-v1-2026-09-04-10.
note_prev2: Kamera UND Galerie frei waehlbar (zwei getrennte Inputs #barfileCam mit capture + #barfileGal ohne). build ms-v8-camgal-6d41e2, sw.js ms-v1-2026-09-04-9.
